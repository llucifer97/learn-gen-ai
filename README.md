# learn-gen-ai
learn gen ai
Here’s a practical GenAI roadmap tailored for you as a backend SDE2, with focus + links.
ssh -i keypair.pem ubuntu@ec2-3-25-153-245.ap-southeast-2.compute.amazonaws.com
nohup ~/rag-venv/bin/streamlit run app.py --server.port 8501 --server.address 0.0.0.0 > rag.log 2>&1 &
deactivate  # Exit venv
tail -f rag.log  # Check
